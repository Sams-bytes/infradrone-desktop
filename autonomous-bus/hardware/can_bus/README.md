# CAN Bus — Qbuzz Bus Interface

TODO once the bus is delivered:
- Document the Qbuzz bus's CAN bus layout (which ECUs, message IDs relevant
  to drive-by-wire: throttle, brake, steering, gear).
- Confirm whether the bus already supports drive-by-wire or whether
  actuation hardware needs to be added.
- DBC files go here once available.

Reference: prior CAN sniffing/tooling experience from the Navya Arma
salvage work may transfer directly (passive monitoring setup, module
identification approach).
